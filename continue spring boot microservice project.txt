Now based on above services, implement each service complete code for running micro services\

Since you're learning from scratch, let's implement a fully runnable Spring Boot Microservices project with:

1. Eureka Server (Service Registry)
2. API Gateway
3. Employee Service
4. Department Service
5. Project Service
6. OpenFeign Communication
7. Eureka Service Discovery
8. Spring Cloud Load Balancer
Architecture
                    Client
                       |
                       V
                 API Gateway
                    :8080
                       |
                Eureka Discovery
                    :8761
                       |
      ---------------------------------
      |               |              |
      V               V              V

 Employee         Department      Project
 Service           Service        Service
 :8081             :8082          :8083
Project Structure
microservices-demo

├── eureka-server
├── api-gateway
├── employee-service
├── department-service
└── project-service
STEP 1 : Eureka Server
pom.xml Dependencies
<dependencies>

    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter</artifactId>
    </dependency>

    <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-starter-netflix-eureka-server</artifactId>
    </dependency>

</dependencies>
EurekaServerApplication
package com.eureka;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.netflix.eureka.server.EnableEurekaServer;

@SpringBootApplication
@EnableEurekaServer
public class EurekaServerApplication {

    public static void main(String[] args) {

        SpringApplication.run(
                EurekaServerApplication.class,
                args);
    }
}
application.properties
server.port=8761

spring.application.name=eureka-server

eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
STEP 2 : Department Service

Port:

8082
Dependencies
spring-boot-starter-web
spring-cloud-starter-netflix-eureka-client
Department
package com.department.entity;

public class Department {

    private Long id;
    private String name;

    public Department() {}

    public Department(Long id, String name) {
        this.id = id;
        this.name = name;
    }

    public Long getId() {
        return id;
    }

    public String getName() {
        return name;
    }
}
DepartmentController
package com.department.controller;

import com.department.entity.Department;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/departments")
public class DepartmentController {

    @GetMapping("/{id}")
    public Department getDepartment(
            @PathVariable Long id) {

        return new Department(
                id,
                "Information Technology");
    }
}
application.properties
spring.application.name=department-service

server.port=8082

eureka.client.service-url.defaultZone=
http://localhost:8761/eureka
STEP 3 : Project Service

Port:

8083
Project
package com.project.entity;

public class Project {

    private Long id;
    private String projectName;

    public Project() {}

    public Project(Long id,
                   String projectName) {

        this.id = id;
        this.projectName = projectName;
    }

    public Long getId() {
        return id;
    }

    public String getProjectName() {
        return projectName;
    }
}
ProjectController
@RestController
@RequestMapping("/projects")
public class ProjectController {

    @GetMapping("/{id}")
    public Project getProject(
            @PathVariable Long id) {

        return new Project(
                id,
                "Banking Project");
    }
}
application.properties
spring.application.name=project-service

server.port=8083

eureka.client.service-url.defaultZone=
http://localhost:8761/eureka
STEP 4 : Employee Service

Port:

8081

This service will call:

Department Service
Project Service

using Feign Client.

Dependencies
spring-boot-starter-web

spring-cloud-starter-netflix-eureka-client

spring-cloud-starter-openfeign
DTO Classes
DepartmentDTO
package com.employee.dto;

public class DepartmentDTO {

    private Long id;
    private String name;

    // getter setter
}
ProjectDTO
package com.employee.dto;

public class ProjectDTO {

    private Long id;
    private String projectName;

    // getter setter
}
EmployeeResponse
package com.employee.dto;

public class EmployeeResponse {

    private Long employeeId;

    private String employeeName;

    private DepartmentDTO department;

    private ProjectDTO project;

    // getter setter
}
Feign Clients
DepartmentFeignClient
package com.employee.feign;

import com.employee.dto.DepartmentDTO;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.*;

@FeignClient(
        name = "department-service"
)
public interface DepartmentFeignClient {

    @GetMapping(
            "/departments/{id}"
    )
    DepartmentDTO getDepartment(
            @PathVariable Long id
    );
}
ProjectFeignClient
package com.employee.feign;

import com.employee.dto.ProjectDTO;
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.*;

@FeignClient(
        name = "project-service"
)
public interface ProjectFeignClient {

    @GetMapping(
            "/projects/{id}"
    )
    ProjectDTO getProject(
            @PathVariable Long id
    );
}
Service Layer
package com.employee.service;

@Service
public class EmployeeService {

    @Autowired
    private DepartmentFeignClient departmentFeignClient;

    @Autowired
    private ProjectFeignClient projectFeignClient;

    public EmployeeResponse getEmployee() {

        DepartmentDTO department =
                departmentFeignClient
                        .getDepartment(1L);

        ProjectDTO project =
                projectFeignClient
                        .getProject(1L);

        EmployeeResponse response =
                new EmployeeResponse();

        response.setEmployeeId(101L);
        response.setEmployeeName("Srini");

        response.setDepartment(department);
        response.setProject(project);

        return response;
    }
}
Controller
package com.employee.controller;

@RestController
@RequestMapping("/employees")
public class EmployeeController {

    @Autowired
    private EmployeeService service;

    @GetMapping("/{id}")
    public EmployeeResponse getEmployee(
            @PathVariable Long id) {

        return service.getEmployee();
    }
}
Main Class
@SpringBootApplication
@EnableFeignClients
public class EmployeeApplication {

    public static void main(String[] args) {

        SpringApplication.run(
                EmployeeApplication.class,
                args);
    }
}
application.properties
spring.application.name=employee-service

server.port=8081

eureka.client.service-url.defaultZone=
http://localhost:8761/eureka
STEP 5 : API Gateway
Dependencies
spring-cloud-starter-gateway
spring-cloud-starter-netflix-eureka-client
spring-cloud-starter-loadbalancer
application.yml
server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

        - id: employee-service
          uri: lb://EMPLOYEE-SERVICE
          predicates:
            - Path=/employees/**

        - id: department-service
          uri: lb://DEPARTMENT-SERVICE
          predicates:
            - Path=/departments/**

        - id: project-service
          uri: lb://PROJECT-SERVICE
          predicates:
            - Path=/projects/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
Run Order
1 Eureka Server
2 Department Service
3 Project Service
4 Employee Service
5 API Gateway
Verify Eureka Dashboard

Open:

http://localhost:8761

You should see:

EMPLOYEE-SERVICE
DEPARTMENT-SERVICE
PROJECT-SERVICE
API-GATEWAY
Test Through Gateway
GET http://localhost:8080/employees/1

Response:

{
  "employeeId":101,
  "employeeName":"Srini",

  "department":{
      "id":1,
      "name":"Information Technology"
  },

  "project":{
      "id":1,
      "projectName":"Banking Project"
  }
}

This demonstrates the complete flow:

Client
  |
API Gateway
  |
Employee Service
  |
  +--> Department Service (Feign)
  |
  +--> Project Service (Feign)
  |
Response

For a real enterprise version, the next step would be adding:

Spring Data JPA + MySQL
CRUD APIs in all services
Global Exception Handling
DTO Mapping
JWT Authentication
API Gateway Security Filter
Resilience4j Circuit Breaker
Config Server
Docker & Kubernetes deployment

These are the components typically used in production-grade Spring Boot microservices.




would be adding:

Spring Data JPA + MySQL
CRUD APIs in all services
Global Exception Handling
Config Server

Perfect. The architecture now becomes a real enterprise microservices architecture.

Enterprise Architecture
                             Client
                                |
                                V
                         API Gateway
                            :8080
                                |
                                V
                         Eureka Server
                            :8761
                                |
      -------------------------------------------------
      |                      |                        |
      V                      V                        V

Employee Service      Department Service      Project Service
     :8081                  :8082                  :8083
      |                      |                      |
      V                      V                      V

 EmployeeDB          DepartmentDB           ProjectDB
   MySQL                MySQL                 MySQL

      |
      |
      V

Config Server :8888
(Git Repository Configuration)
Project Structure
microservices-enterprise

├── config-server
├── eureka-server
├── api-gateway

├── employee-service
│   ├── controller
│   ├── service
│   ├── repository
│   ├── entity
│   ├── dto
│   ├── exception
│   └── feign

├── department-service
│   ├── controller
│   ├── service
│   ├── repository
│   ├── entity
│   ├── dto
│   └── exception

├── project-service
│   ├── controller
│   ├── service
│   ├── repository
│   ├── entity
│   ├── dto
│   └── exception
Step 1: Config Server
Why Config Server?

Without Config Server:

Every service contains its own:

server.port=8081

spring.datasource.url=...

eureka.client.service-url.defaultZone=...

Managing 20+ microservices becomes difficult.

Solution

Store all configuration in one place.

Git Repository
       |
       V
Config Server
       |
       V
Microservices
Config Server Dependency
<dependency>
    <groupId>org.springframework.cloud</groupId>
    <artifactId>
        spring-cloud-config-server
    </artifactId>
</dependency>
Main Class
@SpringBootApplication
@EnableConfigServer
public class ConfigServerApplication {

    public static void main(String[] args) {

        SpringApplication.run(
                ConfigServerApplication.class,
                args);
    }
}
application.properties
server.port=8888

spring.application.name=config-server

spring.cloud.config.server.git.uri=
https://github.com/yourrepo/config-repo
Git Config Repository

Example:

config-repo

employee-service.properties

department-service.properties

project-service.properties

api-gateway.properties
employee-service.properties
server.port=8081

spring.datasource.url=
jdbc:mysql://localhost:3306/employeedb

spring.datasource.username=root
spring.datasource.password=root

eureka.client.service-url.defaultZone=
http://localhost:8761/eureka
Step 2: Employee Service with JPA
Dependencies
spring-boot-starter-web

spring-boot-starter-data-jpa

mysql-connector-j

spring-cloud-starter-openfeign

spring-cloud-starter-netflix-eureka-client

spring-cloud-starter-config
Employee Entity
package com.employee.entity;

import jakarta.persistence.*;

@Entity
@Table(name="employees")
public class Employee {

    @Id
    @GeneratedValue(
        strategy = GenerationType.IDENTITY
    )
    private Long id;

    private String name;

    private String email;

    private Double salary;

    private Long departmentId;

    private Long projectId;
}
Repository
package com.employee.repository;

import com.employee.entity.Employee;

import org.springframework.data.jpa.repository.JpaRepository;

public interface EmployeeRepository
        extends JpaRepository<Employee,Long> {
}
DTO
package com.employee.dto;

public class EmployeeDTO {

    private Long id;
    private String name;
    private String email;
    private Double salary;
}
Service Layer
package com.employee.service;

public interface EmployeeService {

    Employee saveEmployee(
            Employee employee);

    Employee getEmployee(
            Long id);

    List<Employee> getAllEmployees();

    Employee updateEmployee(
            Long id,
            Employee employee);

    void deleteEmployee(
            Long id);
}
Service Implementation
@Service
@RequiredArgsConstructor
public class EmployeeServiceImpl
        implements EmployeeService {

    private final EmployeeRepository repository;

    @Override
    public Employee saveEmployee(
            Employee employee) {

        return repository.save(employee);
    }

    @Override
    public Employee getEmployee(
            Long id) {

        return repository.findById(id)
                .orElseThrow(
                    () -> new ResourceNotFoundException(
                        "Employee Not Found"
                    ));
    }

    @Override
    public List<Employee> getAllEmployees() {

        return repository.findAll();
    }

    @Override
    public Employee updateEmployee(
            Long id,
            Employee employee) {

        Employee existing =
                getEmployee(id);

        existing.setName(
                employee.getName());

        existing.setEmail(
                employee.getEmail());

        existing.setSalary(
                employee.getSalary());

        return repository.save(existing);
    }

    @Override
    public void deleteEmployee(
            Long id) {

        repository.deleteById(id);
    }
}
CRUD Controller
@RestController
@RequestMapping("/employees")
@RequiredArgsConstructor
public class EmployeeController {

    private final EmployeeService service;

    @PostMapping
    public ResponseEntity<Employee>
    create(
            @RequestBody Employee employee){

        return ResponseEntity.ok(
                service.saveEmployee(employee));
    }

    @GetMapping("/{id}")
    public ResponseEntity<Employee>
    getEmployee(
            @PathVariable Long id){

        return ResponseEntity.ok(
                service.getEmployee(id));
    }

    @GetMapping
    public ResponseEntity<List<Employee>>
    getAll(){

        return ResponseEntity.ok(
                service.getAllEmployees());
    }

    @PutMapping("/{id}")
    public ResponseEntity<Employee>
    update(
            @PathVariable Long id,
            @RequestBody Employee employee){

        return ResponseEntity.ok(
                service.updateEmployee(
                        id,
                        employee));
    }

    @DeleteMapping("/{id}")
    public ResponseEntity<String>
    delete(
            @PathVariable Long id){

        service.deleteEmployee(id);

        return ResponseEntity.ok(
                "Deleted Successfully");
    }
}
Global Exception Handling
Custom Exception
package com.employee.exception;

public class ResourceNotFoundException
        extends RuntimeException {

    public ResourceNotFoundException(
            String message) {

        super(message);
    }
}
Error Response
package com.employee.exception;

import java.time.LocalDateTime;

public class ErrorResponse {

    private String message;

    private int status;

    private LocalDateTime timestamp;
}
Global Exception Handler
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(
            ResourceNotFoundException.class)
    public ResponseEntity<ErrorResponse>
    handleNotFound(
            ResourceNotFoundException ex){

        ErrorResponse response =
                new ErrorResponse();

        response.setMessage(
                ex.getMessage());

        response.setStatus(404);

        response.setTimestamp(
                LocalDateTime.now());

        return new ResponseEntity<>(
                response,
                HttpStatus.NOT_FOUND);
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<ErrorResponse>
    handleGeneric(
            Exception ex){

        ErrorResponse response =
                new ErrorResponse();

        response.setMessage(
                ex.getMessage());

        response.setStatus(500);

        response.setTimestamp(
                LocalDateTime.now());

        return new ResponseEntity<>(
                response,
                HttpStatus.INTERNAL_SERVER_ERROR);
    }
}
MySQL Configuration
spring.datasource.url=
jdbc:mysql://localhost:3306/employeedb

spring.datasource.username=root

spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
Department Service

Implement the same layers:

Entity
Repository
Service
ServiceImpl
Controller
Exception

Database:

departmentdb
Project Service

Implement the same layers:

Entity
Repository
Service
ServiceImpl
Controller
Exception

Database:

projectdb
Enterprise Flow
Client
   |
   V
API Gateway
   |
   V
Employee Service
   |
   +------Feign-------> Department Service
   |
   +------Feign-------> Project Service
   |
MySQL
Next Enterprise Features

After completing the above, the production-grade roadmap is:

Service Registry (Eureka) ✅
API Gateway ✅
Config Server ✅
Spring Data JPA + MySQL ✅
CRUD APIs ✅
Global Exception Handling ✅
DTO + Validation (@Valid)
OpenFeign
Circuit Breaker using Resilience4j
Distributed Logging


